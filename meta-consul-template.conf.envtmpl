# We assume 'consul' is running on the localhost
consul = "{{CONSUL_ADDR}}"
retry = "5s"
log_level = "info"

template {
  source      = "/etc/nginx/nginx.conf.ctmpl"
  destination = "/etc/nginx/nginx.conf.pass1"
  command     = "supervisorctl restart consul_template"
}

